# Auth (echte Secrets!)
JWT_ACCESS_SECRET=change_me
JWT_REFRESH_SECRET=change_me
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=30d

NODE_ENV=production
PORT=5001
MONGO_URI=mongodb+srv://<USER>:<PASS>@<CLUSTER>/<DB>?retryWrites=true&w=majority
CORS_ORIGIN=https://<frontend>.onrender.com
SKIP_AUTH=false

MAIL_ENABLED=true            # ⇐ Schalter aktivieren, wenn du testen willst
SENDGRID_API_KEY=SG.xxxxx
EMAIL_FROM="MdxKit <no-reply@deinedomain.de>"
APP_BASE_URL=https://<frontend>.onrender.com
